#pragma once
#include "ChangeNode.h"

void DisplayBoxNode(int x, int y);
void DisplayTreeLinks(Node* root, int x_coord, int y_coord, int width);
void DisplayTree(Node* root, int x_coord, int y_coord, int width);
